{
  "type": "module",
  "scripts": {
    "tsc": "tsc",
    "build": "npm install && npm run tsc",
    "clean": "rm -rf node_modules lib",
    "lint": "npx prettier --check 'src/**/*.{js,ts}'",
    "format": "npx prettier --write 'src/**/*.{js,ts}'",
    "test" : "npx tsx tests/compat.test.ts"
  },

  "types": "lib/index.d.ts",
  "main": "lib/index.js",
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "micro-should": "^0.4",
    "npx": "^10",
    "prettier": "^3",
    "tslib": "^2"
  }
}
